# Brolly v3 Testing Settings Page

This repository hosts the **static settings page used only by Brolly private testing builds**.

It is intentionally separate from the public main settings deployment. The public main settings page is updated only during an explicit **BUILD MAIN** promotion after testing has been approved.

Current hosted settings version: **v3.0.34**.
